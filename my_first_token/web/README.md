# web for My First Token

steps to run web project

* run local web server
* run local network
* deploy to local network
* open browser on web server url


## run local web server

`npx http-server`

## run local network

`npx hardhat node`


## deploy to local network

`npx hardhat ignition deploy --network localhost ignition/modules/my_first_token.ts`



